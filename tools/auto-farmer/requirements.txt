pyautogui>=0.9.54
pillow>=10.0.0
pynput>=1.7.6

# Windows only: most games read DirectInput, which pyautogui's key events do
# not reach. Safe to skip on macOS/Linux.
pydirectinput>=1.0.4; sys_platform == "win32"
